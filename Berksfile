source "https://supermarket.chef.io"

metadata

cookbook 'git'
#cookbook 'nodejs', '= 0.12.7'
#cookbook 'nodejs', '= 2.0.0'
cookbook 'nodejs', path: '../nodejs'

cookbook 'df_node_fix', path: '../df_node_fix'

# you can make sublime work by fixing the ark version dependency collision with the nodejs cookbook
# until then, just use gedit, works fine
#cookbook "sublime-text"
